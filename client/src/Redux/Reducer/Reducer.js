const initialState = {}

const rootReducer = (state = initialState, action) => {
    const { type, payload } = action;
    switch (action.type) {


        
        default:
        return state;
    }
}

export default rootReducer;